# Export
Export is the sync of CMDB Syncer data to a target. Most likely the support for this target is already included in the Syncer. Most users use Checkmk or Netbox as a target. But of course, it's possible to build export plugins in the same way as you build import plugins. They are more complex, since they need more support for rules and require a frontend integration.
